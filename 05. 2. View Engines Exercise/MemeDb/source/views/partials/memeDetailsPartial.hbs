<div class="content">
    <img src={{url}} alt="" />
    <h3> {{title}}</h3>
    <p> {{description}}</p>
    <button>
        <a href="{{url}}" download="{{title}}.jpg">Download Meme</a>
    </button>
    <button>
        <span id="delete" data-meme-id={{id}}>Delete</span>
    </button>
</div>